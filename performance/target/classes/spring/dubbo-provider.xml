<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
	http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <dubbo:application name="performance_provider" owner="zhouxi.zhou" organization="performance"/>

    <dubbo:registry address="${zookeeper_ip}" group="${dubbo_group}"
                    protocol="zookeeper" id="performanceZk" default="true"/>

    <dubbo:protocol name="dubbo" port="${dubbo_port}" accesslog="true" threads="500"/>


    <dubbo:service interface="com.xn.performance.service.PerformanceStressMachineService"
                   ref="performanceStressMachineService" version="${dubbo_version}"
                   timeout="${dubbo_timeout}" registry="performanceZk"/>

    <!-- 和本地bean一样实现服务 -->
    <bean id="performanceStressMachineService"
          class="com.xn.performance.service.impl.PerformanceStressMachineServiceImpl"/>
</beans>
