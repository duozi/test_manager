<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans        http://www.springframework.org/schema/beans/spring-beans.xsd        http://code.alibabatech.com/schema/dubbo        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="manage_platform" />

    <dubbo:registry protocol="zookeeper" address="${zookeeper_ip}" />

    <dubbo:protocol name="dubbo" port="${dubbo_port}" />

    <!-- 声明需要暴露的服务接口 -->
    <!--<dubbo:service interface="" ref="ruleEngineServiceImpl" timeout="${dubbo_ruleengine_timeout}" group="${dubbo_ruleengine_group}" version="${dubbo_ruleengine_version}"/>-->

    <!-- 声明调用的服务接口 -->
    <!--common-test的服务-->
    <dubbo:reference id="companyService" interface="com.xn.common.company.service.CompanyService" timeout="${dubbo_timeout}" group="${dubbo_group}" version="${dubbo_version}" check="false"/>
    <dubbo:reference id="departmentService" interface="com.xn.common.company.service.DepartmentService" timeout="${dubbo_timeout}" group="${dubbo_group}" version="${dubbo_version}" check="false"/>

    <!--interfacetest的服务-->
    <dubbo:reference id="testCaseService" interface="com.xn.interfacetest.service.TestServiceService" timeout="${dubbo_timeout}" group="${dubbo_group}" version="${dubbo_version}" check="false"/>
    <dubbo:reference id="testSystemService" interface="com.xn.interfacetest.service.TestSystemService" timeout="${dubbo_timeout}" group="${dubbo_group}" version="${dubbo_version}" check="false"/>
    <dubbo:reference id="testEnvironmentService" interface="com.xn.interfacetest.service.TestEnvironmentService" timeout="${dubbo_timeout}" group="${dubbo_group}" version="${dubbo_version}" check="false"/>
    <dubbo:reference id="relationServiceEnvironmentService" interface="com.xn.interfacetest.service.RelationServiceEnvironmentService" timeout="${dubbo_timeout}" group="${dubbo_group}" version="${dubbo_version}" check="false"/>
    <!--performance的服务-->
    <dubbo:reference id="performanceStressMachineService" interface="com.xn.performance.service.PerformanceStressMachineService" timeout="${dubbo_timeout}" group="${dubbo_group}" version="${dubbo_version}" check="false"/>
    <dubbo:reference id="performanceMonitoredMachineService" interface="com.xn.performance.service.PerformanceMonitoredMachineService" timeout="${dubbo_timeout}" group="${dubbo_group}" version="${dubbo_version}" check="false"/>
    <dubbo:reference id="performanceScenarioService" interface="com.xn.performance.service.PerformanceScenarioService" timeout="${dubbo_timeout}" group="${dubbo_group}" version="${dubbo_version}" check="false"/>
    <dubbo:reference id="performanceScriptService" interface="com.xn.performance.service.PerformanceScriptService" timeout="${dubbo_timeout}" group="${dubbo_group}" version="${dubbo_version}" check="false"/>

    <dubbo:monitor protocol="registry" />
</beans>